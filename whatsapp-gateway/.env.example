# Generate: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64url'))"
GATEWAY_TOKEN=
# User-supplied number, unchanged; verify through WhatsApp after pairing.
ALLOWED_PHONE=557591568274
